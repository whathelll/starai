# Setup
install nodejs


# Adding to this
```bash
#first run only, installs node modules
npm install

# to start
npm start
```

#### Change html
Modify ./src/index.html
#### Change styles (scss)
Modify ./src/public/sass/starai.scss
